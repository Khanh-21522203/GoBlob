# Scaling

- Add volume servers and point them to master via `-masters`.
- Increase filer replicas behind a load balancer.
- Use `volume.balance` and `volume.fix.replication` from admin shell for maintenance.
