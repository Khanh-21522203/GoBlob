# Security Best Practices

- Keep JWT signing keys rotated and protected.
- Enable IP whitelist for admin/control paths.
- Use TLS for public endpoints.
- Keep rate limiting enabled for exposed interfaces.
- Monitor audit logs for failed auth and mutation spikes.
