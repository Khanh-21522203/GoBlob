# Audit Logging

Audit middleware logs mutation/auth/error requests with:
- method
- path
- status
- remote host
- request duration

Use structured log filters on `"audit"` messages for security review.
