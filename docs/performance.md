# Performance

- Tune `concurrentUploadLimitMB` and `concurrentDownloadLimitMB`.
- Use benchmark suite under `benchmark/` for regression checks.
- Monitor write/read latency histograms on metrics endpoint.
